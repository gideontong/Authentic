/*
 * Function that finds whether or not the current website can
 * be determined to be credible. This code is all but unused
 * and could probably be deleted.
 */

function credibleFinder(){
    var title = console.log(document.title)
}